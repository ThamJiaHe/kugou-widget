# Firebase Configuration
# Get these from Firebase Console > Project Settings > Service Accounts
FIREBASE_CREDENTIALS={"type":"service_account","project_id":"your-project-id","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}
FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com/

# Optional: Kugou API Configuration
# These would be used for additional API calls if needed
KUGOU_APP_ID=your_app_id
KUGOU_SECRET_KEY=your_secret_key

# Development Settings
FLASK_ENV=production
DEBUG=false

# Cache Settings
CACHE_DURATION=60

# Vercel Environment Variables
# Set these in Vercel dashboard under Environment Variables:
# - FIREBASE_CREDENTIALS (paste the entire JSON as a string)
# - FIREBASE_DATABASE_URL (your Firebase Realtime Database URL)

# Instructions:
# 1. Copy this file to .env for local development
# 2. Fill in your actual Firebase credentials
# 3. For Vercel deployment, set environment variables in Vercel dashboard
# 4. Store Kugou user credentials in Firebase at: users/{user_id}/userid and users/{user_id}/token
